# xcode-git-cfbundleversion.sh

Update CFBundleVersion in product Info.plist file with Git commit

## How to use

Add a "Run Script" Build Phase and copy contents of xcode-git-cfbundleversion.sh over.

